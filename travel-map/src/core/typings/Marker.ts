/**
 * Min / max / default scale values used to size map markers relative to zoom level.
 * @property {number} minScale - The minimum marker scale
 * @property {number} maxScale - The maximum marker scale
 * @property {number} defaultScale - The default marker scale
 */
export type MarkerSizes = {
  minScale: number;
  maxScale: number;
  defaultScale: number;
};
